name: deploy

inputs:
  environment: 
    required: true
    
runs:
  using: "composite"
  steps:

  - name: Display file content
    shell: bash
    run: |
      cat test.txt
   
  - name: Display environment
    shell: bash
    run: |
      echo ${{ env.env_name }}
