name: deploy

inputs:
  environment: 
    required: true
    
runs:
  using: "composite"
  shell: bash
  steps:

  - name: Display file content
    run: |
      cat test.txt
   
  - name: Display environment
    run: |
      echo ${{ env.env_name }}
